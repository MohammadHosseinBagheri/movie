import React from 'react';
import {ActivityIndicator} from 'react-native';

const LoadingSpinner = () => {
  return <ActivityIndicator size="large" color="#546E7A" />;
};

export default LoadingSpinner;
